$('#changeid').html("<%= @click_id %>");

$('.next_user_name').text("<%= @user.name %>");

$('.now_user_name').text("<%= @prev.name %>");

$('.prev_user_name').text("<%= @prev.name %>");

$('x-change').html("<%= @x = 7 %>");

